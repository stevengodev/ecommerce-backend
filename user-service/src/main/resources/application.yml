spring:
  application:
    name: user-service
  profiles:
    active:
    - dev
  config:
    import:
    - configserver:http://localhost:8888
    
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
